import React from "react";
import { View, Text } from "react-native";

const AppSelectionScreen = (props) => {
	return (
		<View>
			<Text>this is app selection screen</Text>
		</View>
	);
};

export default AppSelectionScreen;
